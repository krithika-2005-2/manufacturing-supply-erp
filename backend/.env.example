# Existing PostgreSQL database connection string.
# Do not create a new database; point this at the current ERP Postgres instance.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/EXISTING_DB_NAME?schema=public"

JWT_SECRET="replace-with-a-long-random-secret"
JWT_EXPIRATION="8h"

PORT=3000
NODE_ENV=development
